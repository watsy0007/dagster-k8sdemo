execution:
  config:
    image_pull_policy: "Always"

resources:
  io_manager:
    config:
      s3_bucket: "dagster-test"
      s3_prefix: "dagster-k8s"
  s3:
    config:
      # This use of host.docker.internal is unique to Mac
      endpoint_url: http://minio.minio-dev.svc.cluster.local:9090
      region_name: us-east-1
ops:
  multiply_the_word:
    config:
      factor: 0
    inputs:
      word: ""
